[package]
name = "sovereign_engine"
version = "0.1.0"
edition = "2024"

[lib]
name = "sovereign_engine"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.23.4", features = ["extension-module"] }
regex = "1.11.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
